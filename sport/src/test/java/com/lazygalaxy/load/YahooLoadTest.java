package com.lazygalaxy.load;

import junit.framework.TestCase;

public class YahooLoadTest extends TestCase {
	public void testAll() throws Exception {
		// MongoHelper<Team> teamHelper = MongoHelper.getHelper(Team.class);
		//
		// MatchYahooJSoupLoad scraper = new MatchYahooJSoupLoad();
		// Set<String> links =
		// scraper.getLinks("html/yahoo-football-week1-fixtures.html");
		//
		// assertEquals(10, links.size());
		// assertEquals("https://sports.yahoo.com/soccer/premier-league/chelsea-burnley-80000/",
		// links.toArray()[2]);

		// Match match =
		// scraper.getMongoDocument("html/yahoo-watford-vs-newcastle-20180505.html");
		// assertEquals(teamHelper.getDocumentByLabel("Watford").id,
		// match.homeTeamId);
		// assertEquals(teamHelper.getDocumentByLabel("Newcastle").id,
		// match.awayTeamId);
	}

}
